# Dev/test tooling. Install alongside runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
# The constraint below keeps shared/transitive deps at the same pinned versions
# as the runtime set (no divergence between prod and dev).

-c requirements.txt

pytest>=9.1.1
pytest-django>=4.9
pytest-asyncio>=0.24
ruff>=0.15.19
pip-audit>=2.7
