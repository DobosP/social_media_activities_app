# Optional local hooks: `pip install pre-commit && pre-commit install`.
# Mirrors the CI lint/format gate so issues are caught before pushing.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.14
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
