# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenv
.venv/
venv/
env/

# Django
*.log
local_settings.py
db.sqlite3
/staticfiles/
/media/

# Frontend (React/Vite) — built output lands in static/frontend/ (never committed;
# rebuilt in the Docker frontend stage / by `npm run build` in frontend/)
/static/frontend/
frontend/node_modules/

# Env / secrets
.env

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Editors / OS
.idea/
.vscode/
.DS_Store

# Local WSL/Docker helper scripts (session-specific, not for the repo)
.wsl_*

# Local agent worktrees / state
.claude/
docker-compose.local.yml

# Local design/exploration scratch (avatar concept gallery, montages — not for the repo)
scratch/
